# Drop-in example for sshd when using a reverse tunnel to ECS.
# Place under /etc/ssh/sshd_config.d/freemac.conf on the ECS host.
AllowTcpForwarding yes
GatewayPorts no
ClientAliveInterval 30
ClientAliveCountMax 3
